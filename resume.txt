World Bank API
        ↓
HTTP request
        ↓
JSON
        ↓
JSON parsing
        ↓
Python objects
        ↓
DataFrame


1. API request
2. JSON parsing
3. transformar em DataFrame
4. ligar ao Postgres
5. criar tabela com schema correto
6. inserir dados
7. proteger contra duplicados


3️⃣ Porque isso é um problema em pipelines

Em produção, pipelines devem ser:

✔️ idempotentes

podem correr várias vezes sem falhar.

✔️ resilientes

não falham por coisas triviais.

✔️ automatizados

ninguém está a olhar para eles quando correm.


connect_to_database()

ensure_tables_exist()

load_data()

transform_data()

store_results()

1) Migrations — entendeste quase tudo certo

Sim: a lógica é mesmo essa.

Ideia principal

Tens ficheiros versionados no Git que descrevem mudanças ao schema da base de dados.

Exemplo:

migrations/
├── 001_create_macro_data_table.sql
├── 002_add_source_column.sql
└── 003_create_index_on_year.sql

Cada ficheiro diz à BD o que fazer.

Fluxo mental correto
Hoje

Queres criar a tabela:

001_create_macro_data_table.sql
Amanhã

Queres adicionar uma coluna:

002_add_unit_column.sql
Depois

Queres criar um índice:

003_add_index.sql
Porque isto é poderoso

Porque depois alguém faz:

git clone ...

e corre migrations, e a base de dados fica no mesmo estado esperado.

Ou seja, sim:

fica muito mais reproduzível, auditável e controlado

Mas atenção

As migrations são para schema changes, não para “inserir dados normais do pipeline”.

Ou seja:

migrations servem para:

criar tabela

alterar coluna

criar índice

mudar constraints

migrations NÃO servem para:

inserir GDP de Portugal de 2024

carregar dados diários do pipeline


Como vamos fazer isto (abordagem profissional)

Vamos implementar um bulk insert com proteção contra duplicados.

Estrutura:

DataFrame
↓
converter linhas
↓
INSERT INTO macro_data
↓
ON CONFLICT DO NOTHING

Assim o pipeline fica:

idempotente

Ou seja:

podes correr várias vezes
sem duplicar dados


macro-ai-pipeline
│
├── src
│   ├── ingestion
│   │    worldbank.py
│   │
│   ├── processing
│   │
│   ├── database
│   │    db.py
│   │    load_data.py
│   │
│   └── pipeline
│        run_pipeline.py
│
├── tests
│   └── test_db.py
│
├── docker-compose.yml
├── requirements.txt
└── README.md

Data pipelines usually have four layers:

- ingestion: collecting data from sources
- processing: cleaning and transforming data
- storage: persisting data in a database or data lake
- serving: exposing data to applications or analytics

3️⃣ Explicação do pipeline architecture

Isto é super importante profissionalmente.
Quase todos pipelines de dados seguem esta lógica.

Ingestion

Significa buscar dados da fonte.

Exemplos:

API

ficheiros CSV

Kafka stream

base de dados externa

Storage

Guardar os dados num sistema persistente.

Exemplos:

PostgreSQL

S3

data warehouse

parquet files

Processing

Transformar dados.

Exemplos:

limpar dados

calcular métricas

agregações

enriquecer dados

Exemplo neste projeto:

calcular trends macroeconómicas
comparar países
criar features

Aqui normalmente entram ferramentas como:

pandas

Spark

dbt

Tornar os dados consumíveis por utilizadores ou sistemas.

Exemplos:

dashboards

APIs

machine learning models

BI tools

No teu projeto vai ser algo como:


Como o pytest sabe que é um teste?

Não é só o assert. É a combinação de:

1. Nome do ficheiro
test_*.py
2. Nome da função
test_*
3. Uso de assert



🧠 Conceito MUITO importante (nível empresa)

O teu pipeline é agora:

✅ Idempotente
podes correr 10x → resultado consistente
✅ Reprodutível
git clone + docker compose → funciona
✅ Testável
pytest cobre tudo

👉 Isto é literalmente o que recrutadores querem ver.

⚠️ Coisas que normalmente correm mal (contexto real)

Quero mesmo que aprendas isto:

1. Pipeline duplica dados

✔️ resolveste com ON CONFLICT

2. API muda formato

❌ quebra tudo

👉 solução futura:

validação de schema
3. DB não está disponível

❌ pipeline falha

👉 solução:

retry logic
4. Dados vêm vazios

✔️ já viste None

👉 solução:

logging + checks