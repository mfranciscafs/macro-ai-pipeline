services:

  db:
    image: postgres:16
    container_name: macro_postgres

    environment:
      POSTGRES_USER: macro_user
      POSTGRES_PASSWORD: macro_pass
      POSTGRES_DB: macro_db

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: